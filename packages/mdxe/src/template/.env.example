# Payload CMS Configuration
PAYLOAD_SECRET=your-payload-secret-key-here
DATABASE_URL=file:./payload.db

# For MongoDB (alternative)
# DATABASE_URL=mongodb://localhost:27017/mdxe

# For PostgreSQL (alternative)
# DATABASE_URL=postgres://user:password@localhost:5432/mdxe

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# .do Platform Authentication
# Uses the .do platform's WorkOS OAuth (no configuration needed!)
# Authentication is enabled by default for all mdxe projects

# WorkOS Client ID (.do platform - DO NOT CHANGE)
NEXT_PUBLIC_WORKOS_CLIENT_ID=client_01JGQHQ9KZP59W7SGXB7HQMXKD

# OAuth Redirect URI (automatically configured based on environment)
# Development: http://localhost:3000/callback
# Production: https://yourdomain.com/callback (set via NEXT_PUBLIC_SITE_URL)
# NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/callback