---
schemaId: 'https://schema.org/ImageObjectSnapshot'
label: ImageObjectSnapshot
description: >-
  A specific and exact (byte-for-byte) version of an [[ImageObject]]. Two
  byte-for-byte identical files, for the purposes of this type, considered
  identical. If they have different embedded metadata (e.g. XMP, EXIF) the files
  will differ. Different external facts about the files, e.g. creator or
  dateCreated that aren't represented in their actual content, do not affect
  this notion of identity.
parentType: ImageObject
parentUrl: 'https://schema.org/ImageObject'
collection: Types
source: schema.org
version: '27.01'
properties: []
subTypes: []
relatedOccupations: []
relatedIndustries: []
---
# ImageObjectSnapshot

**Schema.org Type:** [`ImageObjectSnapshot`](https://schema.org/ImageObjectSnapshot)
**Parent Type:** [[../ImageObject|ImageObject]]

## Description

A specific and exact (byte-for-byte) version of an [[ImageObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata (e.g. XMP, EXIF) the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity.

## Properties

*Properties inherited from [[../ImageObject|ImageObject]] and specific to this type*

See [[../Properties|Properties]] collection for detailed property definitions.

## Sub-Types

*Types that extend this type*

## Usage Examples

### JSON-LD

```json
{
  "@context": "https://schema.org",
  "@type": "ImageObjectSnapshot",
  "name": "Example ImageObjectSnapshot",
  "description": "Description here"
}
```

### TypeScript

```typescript
import { ImageObjectSnapshot } from 'schema.org.ai'

const item: ImageObjectSnapshot = {
  '@type': 'ImageObjectSnapshot',
  name: 'Example ImageObjectSnapshot',
  description: 'Description here',
}
```

## References

- [Schema.org Documentation](https://schema.org/ImageObjectSnapshot)
- [Schema.org Full Hierarchy](https://schema.org/docs/full.html)
